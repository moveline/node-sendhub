sendHub = {
    config: {}

    username: (username) ->
      @config.username = username

    apiKey: (apiKey) ->
      @config.apiKey = apiKey

    createContact: ({name, number}) ->

    listContacts: ->
}

module.exports = sendHub
